import React from 'react';

export default () => (
  <>
    <iframe
      id="ketcher"
      src="/ketcher"
      title="Ketcher Rails"
      height="730px"
      width="100%"
      style={{border: 'none'}}
      //ref={(f) => { this.ifr = f; }}
    />
  </>
);
