import alt from '../alt';

class ReportActions {
  updateSettings(target) {
    return target;
  }

  toggleSettingsCheckAll() {
    return null;
  }
}

export default alt.createActions(ReportActions);
