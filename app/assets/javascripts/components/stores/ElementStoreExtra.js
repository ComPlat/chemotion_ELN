import {
  extraElementStoreListeners as extraElementStoreListenerslisteners0,
} from 'chemstash';

import {
  extraElementStoreHandlers as extraElementStoreHandlershandlers0,
} from 'chemstash';

const ElementStoreExtra = {
  stateCount : 0,
  listeners0 : extraElementStoreListenerslisteners0,
  listenersCount : 1,
  handlers0 : extraElementStoreHandlershandlers0,
  handlersCount : 1,
};
console.log(ElementStoreExtra);
module.exports = ElementStoreExtra;
