import expect from "expect";

describe('CellLinesFetcher', () => {
    describe('.fetchByCollectionId()', () => {
        it('when no cell line in collection', async () => {
           
        });  

        it('when 2 cell lines are in collection', async () => {
           
        });
    });
    describe('.fetchById()', () => {
        it('when no cell line matches the id', async () => {
           
        });  

        it('when  cell line matches the id', async () => {
           
        });
    });
    describe('.update()', () => {
        it('when params are not valid', async () => {
           
        });  

        it('when params are not valid', async () => {
           
        });
    });
    describe('.create()', () => {
        it('when params are not valid', async () => {
           
        });  

        it('when params are not valid', async () => {
           
        });
    });
});